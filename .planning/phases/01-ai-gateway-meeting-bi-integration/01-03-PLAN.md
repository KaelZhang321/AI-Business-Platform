---
phase: 01-ai-gateway-meeting-bi-integration
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - ai-gateway/app/services/generic_query_executor.py
  - ai-gateway/app/services/text2sql_service.py
  - ai-gateway/app/api/routes/query.py
  - ai-gateway/app/mcp_server/tools.py
autonomous: true
must_haves:
  truths:
    - "通用问数仍通过同一个 public API 可用"
    - "Text2SQLService 可以根据 domain / sub_intent 选择执行器"
    - "HTTP 与 MCP 入口可以显式声明 meeting_bi 域"
  artifacts:
    - "GenericQueryExecutor extracted from current implementation"
    - "Text2SQLService facade/domain resolver"
    - "route and MCP signatures with optional domain"
  key_links:
    - "query route -> Text2SQLService facade"
    - "MCP text2sql -> Text2SQLService facade"
    - "Text2SQLService -> GenericQueryExecutor"
---

<objective>
把当前单体化的 `Text2SQLService` 拆成“统一门面 + 通用执行器”，为会议 BI 执行器挂接留出稳定插槽。

Purpose: 先保证通用问数逻辑被保留下来，再在门面层引入域分流，不直接在旧实现里叠加会议 BI 逻辑。
Output: `GenericQueryExecutor`、重构后的 `Text2SQLService`、支持显式 domain 的 HTTP/MCP 调用面。
</objective>

<execution_context>
@/Users/smart/.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ai-gateway-meeting-bi-integration/01-CONTEXT.md
@docs/plans/2026-03-28-ai-gateway-meeting-bi-integration-design.md
@ai-gateway/app/services/text2sql_service.py
@ai-gateway/app/api/routes/query.py
@ai-gateway/app/mcp_server/tools.py
@ai-gateway/app/models/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract current generic query execution into GenericQueryExecutor</name>
  <files>ai-gateway/app/services/generic_query_executor.py</files>
  <action>将现有 `Text2SQLService` 里的通用 Vanna 初始化、SQL 生成、SQL 执行、UI Spec 生成逻辑抽取到 `GenericQueryExecutor`。保持当前 generic 行为不变，不引入会议 BI 条件判断，也不要让这个执行器依赖 `app/bi/*`。</action>
  <verify>python -m compileall ai-gateway/app/services</verify>
  <done>现有 generic 查询逻辑已从 `Text2SQLService` 中分离，且可以被独立实例化调用。</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Text2SQLService into a domain-routing facade</name>
  <files>ai-gateway/app/services/text2sql_service.py, ai-gateway/app/api/routes/query.py, ai-gateway/app/mcp_server/tools.py</files>
  <action>将 `Text2SQLService` 改造成门面：增加 `resolve_domain()` 与执行器分发；在 HTTP 路由和 MCP tool 中支持可选 `domain` 参数。默认仍走 `generic`，不要在这一计划中接入会议 BI 具体实现，只保留清晰的插槽和兼容的 public interface。</action>
  <verify>python -m compileall ai-gateway/app/api ai-gateway/app/services ai-gateway/app/mcp_server</verify>
  <done>`/api/v1/query/text2sql` 和 MCP `text2sql(...)` 仍通过 `Text2SQLService` 工作；显式 `domain` 入参可透传到门面层；generic 默认路径不回归。</done>
</task>

</tasks>

<verification>
核对 `Text2SQLService` 是否只保留门面职责，不再内嵌所有通用执行细节；确认 `query.py` 和 `tools.py` 两处调用面参数保持一致。
</verification>

<success_criteria>
统一问数入口已经具备域路由能力，后续只需把 `meeting_bi` 执行器接进来即可，不需要再次改动调用面。
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-gateway-meeting-bi-integration/01-03-SUMMARY.md`
</output>
