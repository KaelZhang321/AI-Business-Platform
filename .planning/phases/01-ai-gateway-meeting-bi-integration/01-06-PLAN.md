---
phase: 01-ai-gateway-meeting-bi-integration
plan: 06
type: execute
wave: 4
depends_on:
  - "01-03"
  - "01-04"
  - "01-05"
files_modified:
  - ai-gateway/tests/conftest.py
  - ai-gateway/tests/services/test_intent_classifier.py
  - ai-gateway/tests/services/test_text2sql_service.py
  - ai-gateway/tests/api/test_query_routes.py
  - ai-gateway/tests/api/test_bi_routes.py
autonomous: true
must_haves:
  truths:
    - "generic 与 meeting_bi 两条问数路径都有自动化验证"
    - "固定 BI 路由有基础 API 回归保护"
    - "执行 phase 后可用一组明确命令证明功能没有被重构破坏"
  artifacts:
    - "test skeleton under ai-gateway/tests"
    - "service-level routing tests"
    - "API-level route tests for query and BI endpoints"
  key_links:
    - "IntentClassifier / Text2SQLService domain selection"
    - "/api/v1/query/text2sql domain override"
    - "/api/v1/bi/* endpoint registration and response models"
---

<objective>
补齐第一阶段的最小自动化验证，覆盖统一问数分流与固定 BI 路由。

Purpose: 让会议 BI 融合不是一次性代码搬运，而是带回归保护的可执行 phase。
Output: `ai-gateway/tests` 骨架、服务级测试、路由级测试以及明确的回归命令。
</objective>

<execution_context>
@/Users/smart/.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ai-gateway-meeting-bi-integration/01-CONTEXT.md
@docs/plans/2026-03-28-ai-gateway-meeting-bi-integration-design.md
@ai-gateway/app/services/intent_classifier.py
@ai-gateway/app/services/text2sql_service.py
@ai-gateway/app/api/routes/query.py
@ai-gateway/app/api/routes/bi.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service-level tests for meeting BI intent and domain routing</name>
  <files>ai-gateway/tests/conftest.py, ai-gateway/tests/services/test_intent_classifier.py, ai-gateway/tests/services/test_text2sql_service.py</files>
  <action>创建 `ai-gateway/tests` 基础骨架，为 `IntentClassifier` 和 `Text2SQLService` 编写服务级测试。至少覆盖：会议 BI 关键词落到 `data_meeting_bi`、generic 默认路径、显式 `domain=meeting_bi` 覆盖分流、非会议 BI query 保持 generic。使用 mock/stub 隔离外部模型与数据库，不要求真实连接会议 BI 数据库。</action>
  <verify>pytest ai-gateway/tests/services/test_intent_classifier.py ai-gateway/tests/services/test_text2sql_service.py</verify>
  <done>服务级测试可以自动证明问数域分流逻辑正确，且不依赖真实外部服务。</done>
</task>

<task type="auto">
  <name>Task 2: Add API tests for unified query route and fixed BI endpoints</name>
  <files>ai-gateway/tests/api/test_query_routes.py, ai-gateway/tests/api/test_bi_routes.py, ai-gateway/tests/conftest.py</files>
  <action>补充 API 级测试，覆盖 `/api/v1/query/text2sql` 的显式 domain 透传，以及 `/api/v1/bi/*` 中至少一个汇总接口、一个图表接口和一个明细接口。使用 FastAPI test client 和依赖替换验证路由层行为，不在测试里硬编码旧项目返回包装。</action>
  <verify>pytest ai-gateway/tests/api/test_query_routes.py ai-gateway/tests/api/test_bi_routes.py && python -m compileall ai-gateway/app && ruff check ai-gateway/app ai-gateway/tests</verify>
  <done>统一问数路由与固定 BI 路由均有回归测试；存在一组明确命令可在 phase 收口时重复执行。</done>
</task>

</tasks>

<verification>
确保测试命名、目录结构和 `AGENTS.md` 中的测试约定一致；若需要补充测试依赖或 fixtures，限制在 `ai-gateway/tests` 下完成，不扩散到无关模块。
</verification>

<success_criteria>
会议 BI 融合第一阶段具备最小自动化回归保护，执行阶段结束时可以直接跑测试验证核心链路。
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-gateway-meeting-bi-integration/01-06-SUMMARY.md`
</output>
