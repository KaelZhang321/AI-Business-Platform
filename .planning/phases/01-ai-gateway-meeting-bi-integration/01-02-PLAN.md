---
phase: 01-ai-gateway-meeting-bi-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-gateway/app/bi/__init__.py
  - ai-gateway/app/bi/ai/__init__.py
  - ai-gateway/app/bi/services/__init__.py
  - ai-gateway/app/bi/schemas/__init__.py
  - ai-gateway/app/bi/schemas/common.py
  - ai-gateway/app/bi/db/__init__.py
  - ai-gateway/app/bi/db/session.py
  - ai-gateway/app/bi/db/dependencies.py
autonomous: true
must_haves:
  truths:
    - "会议 BI 域可以作为独立模块被导入"
    - "会议 BI 数据访问不依赖通用问数连接池"
    - "固定 BI 接口与 BI 问数可以共享同一 BI 领域基础设施"
  artifacts:
    - "BI package skeleton"
    - "meeting BI dedicated DB session/dependencies"
    - "shared BI common schemas"
  key_links:
    - "meeting_bi_database_url -> bi.db.session"
    - "bi.db.dependencies -> future route/service injection"
    - "bi.schemas.common -> fixed BI endpoints and BI query executor"
---

<objective>
建立 `app/bi` 的最小领域骨架，让后续 BI 查询执行器与固定 BI 看板接口都能落在同一个独立模块下。

Purpose: 先锁定 BI 域文件边界和数据连接边界，避免后续继续把 BI 逻辑散落到 `app/services`。
Output: 可导入的 `app/bi` 包、独立数据库访问层、共享 BI 基础 schema。
</objective>

<execution_context>
@/Users/smart/.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ai-gateway-meeting-bi-integration/01-CONTEXT.md
@docs/plans/2026-03-28-ai-gateway-meeting-bi-integration-design.md
@ai-gateway/app/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BI package skeleton and dedicated DB access layer</name>
  <files>ai-gateway/app/bi/__init__.py, ai-gateway/app/bi/ai/__init__.py, ai-gateway/app/bi/services/__init__.py, ai-gateway/app/bi/schemas/__init__.py, ai-gateway/app/bi/db/__init__.py, ai-gateway/app/bi/db/session.py, ai-gateway/app/bi/db/dependencies.py</files>
  <action>创建 `app/bi` 包和子模块 `ai / services / schemas / db`。在 `db/session.py` 中基于 `meeting_bi_database_url` 建立会议 BI 专用连接与 session 管理，供固定 BI 接口和 BI 查询执行器共用。不要复用现有 `Text2SQLService` 的 `aiomysql` 连接池，也不要让 BI 域反向依赖 `app/services/text2sql_service.py`。</action>
  <verify>python -m compileall ai-gateway/app/bi</verify>
  <done>`app/bi` 可独立导入；会议 BI 数据连接层存在且与通用问数连接分离；后续 BI 代码有明确落点。</done>
</task>

<task type="auto">
  <name>Task 2: Add shared BI common schema primitives</name>
  <files>ai-gateway/app/bi/schemas/common.py, ai-gateway/app/bi/schemas/__init__.py</files>
  <action>补充 BI 领域的基础 schema，例如通用错误/分页/轻量返回模型或公共类型别名，确保后续固定 BI 接口与 BI 问数执行器都能复用。保持 `ai-gateway` 风格，不复制 `ai-bi-new` 的 `ApiResponse` 包装模型。</action>
  <verify>python -m compileall ai-gateway/app/bi/schemas</verify>
  <done>BI 域存在统一的基础 schema；后续固定 BI endpoint 无需再引入旧项目的响应包装约定。</done>
</task>

</tasks>

<verification>
确认 `app/bi` 中没有反向 import `app/services/text2sql_service.py` 或 `app/api/routes/query.py`，保持领域边界单向依赖。
</verification>

<success_criteria>
BI 域骨架完成，后续固定 BI 接口与 BI 查询执行器都能在 `app/bi` 内独立扩展。
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-gateway-meeting-bi-integration/01-02-SUMMARY.md`
</output>
