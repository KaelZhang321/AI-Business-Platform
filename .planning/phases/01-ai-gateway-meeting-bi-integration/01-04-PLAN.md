---
phase: 01-ai-gateway-meeting-bi-integration
plan: 04
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - ai-gateway/app/bi/ai/context_store.py
  - ai-gateway/app/bi/ai/training_data.py
  - ai-gateway/app/bi/ai/vanna_client.py
  - ai-gateway/app/bi/ai/query_executor.py
  - ai-gateway/app/services/text2sql_service.py
  - ai-gateway/app/services/chat_workflow.py
autonomous: true
must_haves:
  truths:
    - "会议 BI 查询可以通过统一 Text2SQL facade 被执行"
    - "Chat 问数命中会议 BI 时会自动分流到 BI 执行器"
    - "会议 BI 执行器只依赖会议 BI 数据与训练语义，不污染 generic executor"
  artifacts:
    - "Meeting BI context store"
    - "Meeting BI training corpus and vanna client"
    - "MeetingBIQueryExecutor wired into Text2SQLService and ChatWorkflow"
  key_links:
    - "ChatWorkflow sub_intent -> Text2SQLService.query(...)"
    - "Text2SQLService resolve_domain -> MeetingBIQueryExecutor"
    - "meeting_bi settings/db -> Meeting BI vanna client and executor"
---

<objective>
将 `ai-bi-new` 的会议 BI 问数逻辑迁入 `app/bi/ai`，并通过统一门面接入 `ChatWorkflow`。

Purpose: 让会议 BI 问数成为 `ai-gateway` 统一问数体系下的一个受控域，而不是平行入口。
Output: `MeetingBIQueryExecutor`、会议 BI 上下文与训练模块、门面分流接线、Chat 自动分流。
</objective>

<execution_context>
@/Users/smart/.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ai-gateway-meeting-bi-integration/01-CONTEXT.md
@docs/plans/2026-03-28-ai-gateway-meeting-bi-integration-design.md
@ai-bi-new/backend/app/ai/context_store.py
@ai-bi-new/backend/app/ai/training_data.py
@ai-bi-new/backend/app/ai/vanna_client.py
@ai-bi-new/backend/app/ai/query_executor.py
@ai-gateway/app/services/text2sql_service.py
@ai-gateway/app/services/chat_workflow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port and adapt meeting BI AI modules into app/bi/ai</name>
  <files>ai-gateway/app/bi/ai/context_store.py, ai-gateway/app/bi/ai/training_data.py, ai-gateway/app/bi/ai/vanna_client.py, ai-gateway/app/bi/ai/query_executor.py</files>
  <action>从 `ai-bi-new` 迁移会议 BI 的上下文缓存、训练数据、Vanna 客户端和查询执行器逻辑，改写为 `ai-gateway` 配置命名、BI 独立数据库访问和统一响应模型。保留会议 BI 专属的表白名单、问题改写、业务规则训练、业务回答和图表推荐，但不要保留旧项目的独立 API 路由耦合。</action>
  <verify>python -m compileall ai-gateway/app/bi/ai</verify>
  <done>`app/bi/ai` 中存在可独立实例化的会议 BI 查询执行逻辑，且只依赖 BI 域配置和 BI 数据源。</done>
</task>

<task type="auto">
  <name>Task 2: Wire MeetingBIQueryExecutor into the unified query path</name>
  <files>ai-gateway/app/services/text2sql_service.py, ai-gateway/app/services/chat_workflow.py</files>
  <action>在 `Text2SQLService` 中挂接 `MeetingBIQueryExecutor`，由 `resolve_domain()` 选择 generic 或 meeting_bi。更新 `ChatWorkflow._handle_query()`，把 `sub_intent`、`conversation_id`、`context` 一并传入 facade。保持规则为“Chat 自动分流，Direct API / MCP 显式分流”，不要在本计划里引入第二条 BI AI 查询主入口。</action>
  <verify>python -m compileall ai-gateway/app/services</verify>
  <done>命中 `data_meeting_bi` 的聊天问数会通过统一 facade 走 BI 执行器；非会议 BI query 继续走 generic；调用面仍只有一个 `Text2SQLService`。</done>
</task>

</tasks>

<verification>
检查 `ChatWorkflow` 没有直接 import `app/bi/ai/query_executor.py`，而是始终经由 `Text2SQLService` 统一分发。
</verification>

<success_criteria>
会议 BI 问数已经接入统一问数体系，且 generic 与 meeting_bi 两条执行路径在代码结构上清晰隔离。
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-gateway-meeting-bi-integration/01-04-SUMMARY.md`
</output>
