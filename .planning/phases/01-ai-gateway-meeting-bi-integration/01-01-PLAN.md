---
phase: 01-ai-gateway-meeting-bi-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-gateway/app/models/schemas.py
  - ai-gateway/app/core/config.py
  - ai-gateway/app/services/intent_classifier.py
autonomous: true
must_haves:
  truths:
    - "统一问数契约可以表达会议 BI 域信息而不破坏现有调用方"
    - "意图分类可以将会议 BI 查询与通用 query 区分开"
    - "会议 BI 独立配置可加载且不覆盖通用问数配置"
  artifacts:
    - "domain-aware Text2SQL request/response models"
    - "meeting BI dedicated settings"
    - "intent classifier rules for data_meeting_bi"
  key_links:
    - "IntentClassifier -> SubIntentType.DATA_MEETING_BI"
    - "Settings -> Text2SQLService / Meeting BI executor"
    - "Schemas -> HTTP / Chat / MCP callers"
---

<objective>
建立会议 BI 融合的最小公共契约：让共享 schema、配置和意图分类先支持 `meeting_bi` 域。

Purpose: 为后续门面重构、执行器分流和 BI 路由接入提供稳定基础。
Output: 可承载 `meeting_bi` 的请求/响应模型、独立配置项、新的二级意图。
</objective>

<execution_context>
@/Users/smart/.agents/skills/gsd/workflows/execute-plan/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ai-gateway-meeting-bi-integration/01-CONTEXT.md
@docs/plans/2026-03-28-ai-gateway-meeting-bi-integration-design.md
@ai-gateway/app/models/schemas.py
@ai-gateway/app/core/config.py
@ai-gateway/app/services/intent_classifier.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared query schemas for domain-aware routing</name>
  <files>ai-gateway/app/models/schemas.py</files>
  <action>新增 `SubIntentType.DATA_MEETING_BI`，扩展 `Text2SQLRequest` 与 `Text2SQLResponse` 以承载 `domain`、`conversation_id`、`answer` 等会议 BI 需要的信息。保持现有 HTTP / Chat / MCP 调用方向后兼容，不要修改已有必填字段的语义，也不要把 BI 特定字段塞进 `ChatResponse`。</action>
  <verify>python -m compileall ai-gateway/app/models</verify>
  <done>共享 schema 编译通过；`Text2SQLRequest` 可表达显式域选择；`Text2SQLResponse` 可表达域归属和业务回答。</done>
</task>

<task type="auto">
  <name>Task 2: Add meeting BI settings and classifier rules</name>
  <files>ai-gateway/app/core/config.py, ai-gateway/app/services/intent_classifier.py</files>
  <action>在配置中增加 `meeting_bi_*` 独立设置，覆盖数据源、模型、训练与上下文保留等需求。更新意图分类 prompt 和关键词兜底，使会议、报名、签到、客户画像、大区、ROI、方案情报等问题落到 `query + data_meeting_bi`，同时避免把泛化的客户/销售/运营查询全部误判为会议 BI。</action>
  <verify>python -m compileall ai-gateway/app/core ai-gateway/app/services</verify>
  <done>Settings 可以提供独立会议 BI 配置；IntentClassifier 可以返回 `data_meeting_bi`；既有 `knowledge/task/query/chat` 一级分支不被破坏。</done>
</task>

</tasks>

<verification>
检查 `schemas.py`、`config.py`、`intent_classifier.py` 三者的字段名和枚举值一致，避免后续 facade 分流时出现字符串不匹配。
</verification>

<success_criteria>
`meeting_bi` 域的请求/响应契约、配置项和意图分类能力准备完成，后续计划无需再改公共命名。
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-gateway-meeting-bi-integration/01-01-SUMMARY.md`
</output>
